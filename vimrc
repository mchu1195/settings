filetype indent plugin on "automatically determine files
syntax on "enable syntax processing

set tabstop=4 "4 spaces per tab
set shiftwidth=4 "keep shift in line with tabs
set softtabstop=4 "delete tabs
set expandtab "tabs as spaces
set autoindent

set wildmenu "autocomplete cmds
set cursorline "highlight the current line
set number "show line numbers
set ruler  "display cursor position

set hlsearch "highlight on search
set ignorecase "case insensitive search
set smartcase  "unless I specifically use capital letters

set mouse=a "enable the mouse

set backspace=indent,eol,start "allow backspace to wrap around lines
set whichwrap+=<,>,h,l,[,] "arrow keys going past lines

set linebreak "dont wrap lines in the middle of a word
